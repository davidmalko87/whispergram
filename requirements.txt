faster-whisper>=1.0
tqdm>=4
# ffmpeg must be installed separately (system package), not via pip.
# For NVIDIA GPU on Windows, also: pip install nvidia-cublas-cu12 nvidia-cudnn-cu12 "ctranslate2>=4.5"
